import React from 'react';

const LazyLoadingFirstExample = () => {
    return (
        <div>
            <h4>The Intersection Observer API is often used in vanilla JavaScript for implementing lazy loading, efficiently loading content as it comes into view during scrolling. However, in React, lazy loading is typically easier to implement thanks to the framework's built-in features and libraries like React Lazy and React Suspense.</h4>
            <h5>Lazy Loading used to increase the loading speed of application with dynamic importing concept.</h5>
        </div>
    );

}
export default LazyLoadingFirstExample;

